// This example demonstrates an iterative approach to calculating Fibonacci numbers.
fun fibonacciIterative(n: int) -> int:
    if (n <= 1):
        return n
    end

    a: int = 0
    b: int = 1    
    for (i in [2..n]):
        temp: int = b
        b = a + b
        a = temp
    end
    return b
end

print(fibonacciIterative(10))
