#include <std/assert>

// test functions

println("hello!")
assert_eq!(len("this is 26 characters long"), 26)

// styled prints
styledprint("hello", bold=true)
styledprint("hello", italic=true)
styledprint("hello", underline=true)
styledprint("hello", blink=true)
styledprint("hello", reverse=true)
styledprint("hello", strikethrough=true)
styledprint("hello", link="https://example.com")  // OSC 8
styledprint("hello", fg_color="red")
styledprint("hello", bg_color="blue")
// ik end may be highlighted here but it is allowed
styledprint("hello", end="🌸")

assert_eq!(type_of(123), int)
assert_eq!(range(5).collect(), ([0, 1, 2, 3, 4]))
assert_eq!(range(2, 5, as=list), ([2, 3, 4]))
assert_eq!(range(2, 10, 2, as=tuple), (2, 4, 6, 8))

help()
warn("this is a warning")
exit()
